# Disable optional dependencies to save memory
omit=optional

# Increase network timeout
network-timeout=100000

# Use legacy peer deps to avoid issues
legacy-peer-deps=true

# Reduce memory usage
cache-min=3600
fund=false
audit=false
